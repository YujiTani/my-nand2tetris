/* nand2tetrisで使うhdlのテンプレートを作成する */
/*
example: Xor (exclusive or) gate: If a!=b out=1 else out=0.
*/
CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    // Put your code here:
    Xor(a=a, b=b, out=out);
}
